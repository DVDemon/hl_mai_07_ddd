#include <iostream>

/*
public void saveClient(String clientId, String clientFirstName, String clientLastName, String streetAddress, String phoneNumber, String emailAddress) {
    
    Client = clientDao.readClient(clientId);
     
    if (client == null) {
        client = new Client();
        client.setClientId(clientId);
    }
    
    if (clientFirstName != null) {
        client.setClientFirstName(clientFirstName);
    }
    
    if (clientLastName != null) {
        client.setClientLastName(clientLastName);
    }
    
    if (streetAddress != null) {
        client.setStreetAddress(streetAddress);
    }
    
    if (phoneNumber != null) {
        client.setPhoneNumber(phoneNumber);
    }
    
    if (emailAddress != null) {
        client.setEmailAddress(emailAddress);
    }
    
    clientDao.saveClient(client);
}

*/
auto main() -> int {
    return 0;
}